# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

# Environments
.venv/
venv/
env/

# Editors and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Generated at run time. runs/baseline.json is committed on purpose -- it is the
# baseline every future run is compared against.
runs/*.json
!runs/baseline.json

# Only written when no expectations.jsonl exists yet, to be filled in and saved
# under that name. The template itself is scratch; the filled-in file is not.
*/expectations.template.jsonl

# Benchmark data. Fetched with benchmarks/fetch_data.py; carries its own licence
# and is not this project's to redistribute. Only benchmarks/results.md is
# committed.
_data/

