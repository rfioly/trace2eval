[English](DECISIONS.en.md) | **简体中文**

---

# 设计决策

这份文档记的是**为什么**，不是**做了什么**。做了什么代码里写着，这里只记那些本来完全可能走向另一边的选择、把它们定下来的证据，以及证据其实并不够的地方。

先说一句：这是某一个时间点的快照。里面的数都是在 42 行样本上量的，我自己也还在改。**如果某条结论跟你手上的数据对不上，大概是我的样本太小，不一定是你错了。**

## 先说我最后定下来的几个选择

- 相似度用**重叠系数**，不用 Jaccard
- 去重阈值 **0.6**，暴露成 `--dedup-threshold`
- 聚类跑**整份日志**，不是只跑候选集
- 簇内比对用**单链接**，不是质心；比对对象是**不同问法**，不是每个成员
- shingle 用**字符二元组**，不做分词——但相似度本身**可以换**（`--similarity`）
- **空输出不算坏行**，算信号
- **参考输出不等于标准答案**：形状从同一问题的**干净答案**里推
- 每个用例**拿自己的参考输出做自检**，结论写进报告
- **不做 LLM 裁判**
- 容差：通过率和格式合规用绝对百分点，延迟和成本用相对百分比

下面挨个说。最后几条是 v0.2 补的，也一并说了——v0.1 那版留着不少欠账。

---

## 为什么最后没用 Jaccard

**常规做法是 Jaccard，但在这份数据上它把两对样本的排序排反了。**

下面几对都是 `examples/sample_traces.jsonl` 里的真行，人工标过。

| 样本对 | 重叠系数 | Jaccard | 是同一个问题吗 |
| --- | --- | --- | --- |
| `你们的退款政策是什么？` vs `退款政策` | 1.000 | 0.333 | **是** |
| `你们的退款政策是什么？` vs `退款政策是怎样的` | 0.571 | 0.333 | **是** |
| `修改手机号` vs `我要改手机号` | 0.750 | 0.500 | **是** |
| `支持哪些登录方式` vs `支持哪些支付方式` | 0.571 | 0.400 | **不是** |
| `登录不上` vs `支持哪些登录方式` | 0.000 | 0.111 | 不是 |

看第三行和第四行。Jaccard 给一对**不同**的问题（0.400）打的分，比任何一对**同义**问法（0.333）都高。排序错了，那调阈值就没意义了，只能换度量。

为什么会这样：Jaccard 除以并集，于是惩罚一切长度差异。而用户的短查询，几乎永远是一个更长问法的碎片。重叠系数除以的是较短的那个集合，问的才是我们真正关心的事——短的那个，是不是被包在长的里面？

**这里得老实交代。** 重叠系数把顺序修对了，但并**没有**干净地分开最后两行：真配对和假配对都落在 0.571。表面相似度本身分不出它们。这不是调阈值能解决的，是字符 n-gram 在短中文文本上的极限。我目前的处理方式是让阈值偏向精确率，被牺牲的召回率靠单链接聚类补回来——下面还会再提到这点。

由 `tests/test_dedup.py::test_jaccard_ranks_the_false_pair_higher` 钉住。谁要是把度量换回去，那个测试会失败，并说明原因。

---

## 0.6 这个数是怎么来的

在样本日志上扫 `--dedup-threshold`，然后一个一个簇人工看：

| 阈值 | 合并出的簇 | 折叠掉的行 | 结论 |
| --- | --- | --- | --- |
| 0.50 | 6 | 14 | 把 `支持哪些登录方式` 和 `支持哪些支付方式` 合并了，这俩不是一回事 |
| 0.55 | 4 | 11 | 同一对假配对还是被合并 |
| **0.60** | **3** | **9** | 样本上没有假配对了 |
| 0.65 | 2 | 5 | 开始漏真重复 |
| 0.70 | 2 | 5 | |
| 0.80 | 1 | 3 | 大部分重复活下来，去重几乎等于没做 |

0.6 是精确率干净、同时又没丢掉关键簇的那个点。

但这是在一份 42 行样本上试出来的，**很可能有更好的定法，我只是还没找到**。所以它是暴露出来的参数。真实部署应该拿自己流量上人工标的配对再扫一遍——其实这跟这个工具对输出质量做的事是同一件，只不过反过来用在工具自己身上。

---

## 聚类为什么跑全量，不跑候选集

最容易想到的写法是：给每条 trace 打分，留下高于最低分的，再对这份短名单去重。它会产出一个**不诚实的数字**。

一个被问了 200 次、只崩过 1 次的问题，"先筛后去重"会报"出现 1 次"。先给全量聚类才能报"出现 200 次"——而后者才是你能真写进报告的那句话。

所以流程是：全部打分 → 全部聚类 → 再决定哪些簇值得提拔。每个簇的代表是它分数最高的成员，因为排序保证了最糟的那一行最先认领这个簇。

---

## 为什么是单链接，不是质心

每个簇会跟它的**每一个成员**比对，不只是跟代表比。

不做这件事链就断了。`我想了解退款政策` 跟那个长的代表 `你们的退款政策是什么？` 只有 0.43，但跟已经在簇里、更短的 `请问退款政策` 有 0.60。用质心会把它丢掉，单链接留得住。

代价是相似度不满足传递性，一个松散的簇可能顺着链越滚越大。这里靠 `min_shared` 兜着，另外失败 trace 排在最前面、会早早把簇锚住，所以风险还算可控。但我不敢说这个约束足够强。

## 为什么是字符二元组

一套代码路径要同时服务中文和英文。中文没有空格分词边界，按词切就得引分词器和词典，对一个把"零依赖"当卖点的工具来说包袱太重。

用二元组不用三元组，是因为查询都很短：一个五字的问题切三元组之后，几乎剩不下能匹配的东西。

代价是英文改写场景召回弱。`"how do I get a refund"` 和 `"refund process"` 有共享 n-gram，能匹配上；`"getting my money back"` 匹配不上。中文分词那块我了解得不深，选二元组有一半原因是不想引依赖，所以这个短板算是我自己偷懒的结果，不是深思熟虑的取舍。

---

## 空输出这件事，我改过一次

第一版对一次调用的两侧复用了同一个"这个值在不在"的辅助函数，结果它把 `"output": ""` 当成缺失字段，然后把这行跳过了。

那等于把工具存在的唯一理由悄悄删掉了。空的**输入**才是坏行，没东西可答；空的**输出**完全可读，而且是日志里最有价值的行之一。

现在两种情况分开处理（`_pick(..., allow_empty=True)`），由 `test_empty_output_is_a_trace_not_an_unreadable_line` 钉住。

---

## 参考输出不等于标准答案

这条塑造了整个用例集。而且我改过两次才改到现在这个写法。

核心问题是：大多数被选中的 trace，恰恰是因为它出了错才被选中的。那从它的输出里推"期望形状"，基本就是从失败里学标准。

规则最后定成这样：

- **干净 trace**（`negative_feedback`、`user_retried`、`empty_output`、`fallback_phrase`、`expected_shape_violated`、`output_much_shorter` 一个都没触发）：输出可以当作假定正确的参考。按它长度的 40% 推 `min_chars`；如果能解析成 JSON 对象，再推一个 schema。
- **失败种子**（上面任意一个触发了）：只断言 `not_fallback`，别的都不加。这个用例存在的意义，就是让这一次具体的失败永远不要再上线第二次。

`min_chars` 的 40% 是拍的。目的在抓**截断**，不是惩罚改写——凡是会因为合理改写就报红的检查，活不过一周就会被人关掉。

### 为什么不从参考输出推 `contains`

很诱人，但错。从某条参考输出自动生成 `contains: ["14 个工作日"]`，等于把测试过拟合到一种措辞上。之后任何正确但换了说法的回答都会失败，测试套件变噪音，然后团队就不信它了。

trace 自己带了显式 `contains` 的话我们是照办的——因为那时候那些词是人选的。

这块我留过一个明显的口子：失败种子只断言"不许兜底"，所以一个**错得不一样**的答案照样过。样本日志里就能复现——把某个失败种子的答案换成 `订单处理中。`，门禁抓不到。说白了，"参考输出不是标准答案"这条原则我定对了，落地只落了一半。

---

## v0.2 把那一半补上了（外加两个副作用）

补法的核心是**换参考源**：形状从"同一个问题的干净答案"里推，不从那一条推。

日志里"退款政策"被问了六种问法，其中一条被标了差评。那条是失败种子，不能当参考；但另外六条是干净的，它们的中位数就能当长度下限。一条干净答案都没有时还有一层兜底：如果失败本身就是"答案太短"，那它是从日志的短答线掉下去的，就拿那条线当下限。

这层兜底得说清楚，它**是弱参考**：来自全日志的中位数，不是这个名字的问题的中位数，所以对"本来答案就短"的问题可能过严。`report.md` 会把这类用例的形状来源标成"日志的短答线（弱参考）"，调阈值时第一个该看的就是它们。

顺带修了一个隐患。短答线原本只是 `signals.py` 里的一个条件表达式，现在抽成了 `suspiciously_short_boundary()`，信号和检查都从它取数。不抽的话，哪天改了阈值而检查没跟着改，用例会**悄悄**不再复现自己的失败——这种坏法最难发现。

**副作用一：用例开始自检。** 每个用例都拿自己的参考输出跑一遍自己的检查，结果写进 `report.md` 和 `cases.jsonl`。判定分三种：

- 失败种子**没通过**自己的检查 → 正常，它复现了当初那个失败
- 失败种子**通过**了自己的检查 → `failure_not_reproduced`，这组检查根本抓不住它
- 干净用例没通过自己的检查 → `reference_fails_own_checks`，要么参考错了，要么检查错了

第二种就是那个残余短板的**量化**版本。样本日志 16 个用例里有 3 个落在这一类，原因一模一样：`user_retried`——用户又问了一遍，而输出本身没毛病。任何对文本的确定性检查都抓不到"用户会重问"。这是方法的边界，不是活儿没干完；要真拦住它，得有一个**人工标注的期望答案**，而这个工具不产出那个。

我没打算把这 3 个数字压下去。能压下去的办法只有一个：加几条会误报的检查——而那正是让人把整个门禁关掉的原因。

**副作用二：成本不再随重复次数增长。** 原来是拿新来的跟簇里的**每一个成员**比，一条问题问 5000 次就要比 5000 次。现在簇里记的是**不同问法**（指纹去重），5000 条一模一样的问题只留 1 个待比对指纹。上限 `MAX_DISTINCT_FINGERPRINTS = 512` 是我拍的保护值：超过之后多余的会另起一个簇，于是多出来的是**重复用例**而不是**漏掉的用例**。宁可重复，不可漏。

---

## 相似度可以换掉，但默认不换

字符 n-gram 的死穴是固定的：两个不含相同字符的改写得 0 分。要修就得引 embedding，而这个包的卖点是零依赖。

所以不修，改成**可换**：`--similarity module:function` 指向你自己的 `(str, str) -> float`。想上 embedding 就上，包不动。随包带了两个现成的替代匹配器，以及一条实测出来的反例——在中文上 `word_jaccard` 比默认的**更粗**（`支持哪些登录方式` vs `支持哪些支付方式` 从 0.571 涨到 0.857），所以它是个能跑的例子，不是推荐值。

有个代价得说明白：一换自定义匹配器，n-gram 倒排索引就停用了。那个索引是**按字面**筛候选的，留着它正好会把你想找的语义召回卡死。停用之后就是拿新来的跟每个簇比，大日志上会明显变慢。

---

## 那 3 个行为型用例，最后是这么处理的

v0.2 把弱检查用例从 9/16 压到 3/16，剩下 3 个全是 `user_retried`：用户又问了一遍，**答案本身没毛病**。

我一开始想从代码上解决它，想了几个方案都不成立——没有一个能绕开这个事实：**失败不在文本里**。对文本做检查，怎么调都抓不到"用户会重问"。

所以方向换成**补上人手写的那部分**。工具负责三件事：认出这些用例、生成待填模板、把标注合并进检查。人负责一件事：写下正确答案长什么样。

### 为什么按问题索引，不按用例编号

这是整个功能里唯一真正难的技术点，其余都是管道。

用例编号（`case-011`）是按分数降序**临时分配**的。往日志里加一条高分 trace，所有编号往后挪一格，挂在编号上的标注就静默失效了。

而**一个脱钩的标注比没有标注更糟**：那个用例看起来还有覆盖，实际什么都没断言。这种坏法不会报错、不会变红，只会在某天门禁放过去一个显然的回归时才被人发现。

所以标注的键是**问题本身**（`input`，折叠大小写、空白、标点后比对）。`test_an_annotation_follows_the_question_when_case_ids_shift` 专门盯这条：它故意加一条高分 trace 把编号全部打乱，再断言标注还挂在同一个问题上。

### 标注不改变自检结论

写这份文档时我差点把它说成"标注修好了那 3 个用例"。不是的。

自检问的是"这组检查能不能在那条出错的输出上失败"。行为型失败的输出是**好的**，所以答案必然是不能——加了标注还是不能，verdict 依然是 `failure_not_reproduced`。

变的是**目标**：从这个用例"复现当初那个失败"，变成"答案必须满足这份规格"。前者做不到，后者能做到，而且后者才是真能拦住回归的那个。

报告里这几个数**分开写**：有几个抓不住自己的失败、其中几个已有标注覆盖、还剩几个要人写。我特意没把它们合成一个"覆盖率"——那种数字好看，但会掩盖"其实还有 3 个抓不住"这件事。

### 一个我本来搞错的细节

`expect` 里如果人只写了 `contains` 而没写 `min_chars`，合并时**要保留推断出来的长度检查**。

我最初的直觉是"人工标注应该整体覆盖推断结果"。但那样的话，人写一条 `contains` 会**顺手拆掉**截断检查——等于标注让用例变弱了。一个功能不该有这种行为。现在的规则是按检查类型**逐项**覆盖：人写了的用人的，人没提的保留推断值。

这条是写测试时才想明白的。我在测试里拿一个 6 字的坏参考配 `min_chars: 20`，发现检查**反而**能命中、verdict 翻成了 `ok`——这个"反例"让我意识到 verdict 的两个分支都成立、只是情形不同：参考是好答案时保持 `failure_not_reproduced`，参考真是坏答案时翻成 `ok`。于是补了一条测试把另一个分支也钉住。

---

## 去重阈值终于有了外部证据（v0.4）

之前所有数字都是在**我自己写的 42 行日志**上量的。我按自己想验证的特征去设计它，再拿结果当实测——循环论证。现在换成 SQuAD 问句 + 人工改写的数据集（`benchmarks/`），配对标签来自数据构造本身，不是我的判断。

结论分两半。

**0.6 站住了。** 在 1062 条带标注数据上，它在最难的负样本类别上正好是最优值：召回 1.000，对无关问句的误报率 0.004。这是好消息——默认值不用改。

**但单链接把 0.4% 放大成了灾难。** 拿 1062 条**互不重复**的问句进去，0.6 的配对误报率只有 0.4%，可结果是 **54% 的问句被并进某个簇，最大簇 425 条**。原因不复杂：1062 条里有约 56 万对，0.4% 就是两千多条虚假连接，足够把整个池子串起来。0.8 能把被并入比例压到 1.8%，但召回会掉——而这里的召回本来就被高估了（见下）。

**对抗集给出了硬上限。** 那 56 对是专门为"让字面匹配答错"构造的。0.6 之上有 98% 被判为同一问题。这是字符 n-gram 这条路的天花板，不是我实现得不好。

### 中间我搞错了一次，而且错得很彻底

那个数据集的两个文件都带 qa `id`，所以我按 `id` 配对——**但它们是按位置对齐的，id 被重新生成过。**

于是我得到一份完整的、看起来很专业的评估表：阈值 0.6 召回率 0.006。**全部是噪声。** 如果我当时直接写进 README，那就是把一条彻底的假结论当成"实测"发布出去。

发现它的是这一步：**所谓"正样本"的相似度分布（median 0.343）和随机配对的分布（median 0.345）一模一样。** 标签没起作用。

现在这条检查固化进了 `dedup_eval.py`：**如果正样本不比随机对更接近，脚本直接拒绝出报告并退出。** 静默的错配比崩溃危险得多。

### 一个我写出来又删掉的告警

基于上面的发现，我给 `build` 加了个聚类健康告警：簇里某个问法离代表太远，就提示可能被错误合并。

**它当场在我自己的样本日志上误报了。** 被判"链式合并"的那个簇里是：

```
你们的退款政策是什么？ / 退款政策是什么 / 请问退款政策 / 我想了解退款政策 (0.429)
```

全是同一个问题。而 `我想了解退款政策` 只有 0.429——它正是文档里明确举过的那个合法短碎链条。

也就是说：**"短碎问法离代表很远"既是合法链条的特征，也是错误合并的特征，从这个指标上分不开。** 会误报的告警活不过一周，所以我删掉了告警，只保留测出来的数字（最大簇多大、多少比例被并入），并在 `ClusterHealth` 的注释里说明为什么不做自动判断。

判断留给看报告的人——而报告里给了参照值：同样跑法在 1062 条无重复问句上，最大簇 425 条。

### 这次评估没有覆盖什么

- **语言是英语。** 工具针对中文日志，而这个结果是对机制的证据，不是对目标语言的。
- **改写是机器生成的、改动很小**（中位相似度 0.968）。正样本比真实改写容易得多，所以召回偏乐观、最优阈值偏大。"0.6 是最优"不能读成对人类改写的结论。
- **信号层完全没测。** 差评、重试、延迟、成本决定哪些 trace 值得测，而公开数据里没有一份同时具备文本和运维字段。所以"什么值得测"这一半仍然只有那 42 行样本日志。

---

## 为什么不做 LLM 裁判

v0.1 主动拒了，三条理由：

1. **CI 里的成本和延迟。** 每个 PR 跑一遍裁判模型，是一笔账单也是一个瓶颈，而且是构建一旦变慢后第一个被关掉的东西。
2. **不确定性。** 这个工具的全部价值就是"同一份日志出同一套用例"，裁判会破坏它。
3. **已经被解决了。** DeepEval、Ragas、promptfoo 都做得挺好，再造一个正是这个项目想避开的坑。

不过这个取舍**我不太确定是不是最优解**。有了裁判模型能测的东西确实更多，也许对某些场景是更划算的。但目前这个选择换来的是确定性和零依赖，我觉得值。

---

## 容差为什么一半绝对、一半相对

通过率和格式合规率用**绝对百分点**（2pp）。"掉了 2% 的通过用例"在任何基线下含义都一样。

p95 延迟和平均成本用**相对百分比**（20%）。"慢了 20%""贵了 20%"要跟着现有数字的规模走。

`--tolerance-scale` 统一缩放所有容差，所以门禁想更严或更松都不用改代码。这九条里这条大概是最不重要的，放这儿只是因为总得有人决定一下。

---

## 接下来做什么

v0.1 列的三件事，前两件在 v0.2 做掉了。v0.2 列的第一件——给行为型用例补人工标注——在 v0.3 做掉了。

按"对工具的提升幅度"排：

1. **去重阈值还是拍的。** 0.6 是在 42 行上调出来的。几百对人工标注的配对、每个阈值算一遍精确率和召回率，才能把这个合理的猜测变成站得住的默认值。这其实还是这个工具在吃自己的狗粮——它给别人输出打分的逻辑，正好能拿来给它自己的阈值打分。

2. **在真实规模的日志上跑一遍。** 十万行以上，看两件事：`MAX_DISTINCT_FINGERPRINTS = 512` 够不够，以及换自定义匹配器之后（索引停用）到底慢到什么程度。这两个数我到现在还是估的，没有实测。

3. **让标注能"提议"，而不是从零写。** 现在人得手写完整的 `expect`。如果工具能先给个草稿——比如从同一问题的干净答案里提几个候选关键词——人只需要"确认 / 否掉"，会快很多。但我不确定这种草稿的质量，可能反而添乱。这个想法我还没验证过。
