[English](DECISIONS.en.md) | **简体中文**

---

# 设计决策

这份文档记录的是**为什么**，不是**做了什么**。做了什么，代码里写着。这里记的是那些本来完全可能走向另一边的选择、把它们定下来的证据，以及证据其实并不充分的地方。

---

## 1. 相似度用的是重叠系数，不是 Jaccard

**常规选择是 Jaccard。但在这份数据上，它把两对样本的排序排反了。**

下面两对都是 `examples/sample_traces.jsonl` 里的真实数据，人工标注过。

| 样本对 | 重叠系数 | Jaccard | 是同一个问题吗 |
| --- | --- | --- | --- |
| `你们的退款政策是什么？` vs `退款政策` | 1.000 | 0.333 | **是** |
| `你们的退款政策是什么？` vs `退款政策是怎样的` | 0.571 | 0.333 | **是** |
| `修改手机号` vs `我要改手机号` | 0.750 | 0.500 | **是** |
| `支持哪些登录方式` vs `支持哪些支付方式` | 0.571 | 0.400 | **不是** |
| `登录不上` vs `支持哪些登录方式` | 0.000 | 0.111 | 不是 |

Jaccard 给那一对**不同**的问题（0.400）打出了比任何一对**同义**问法（0.333）都高的分。**任何阈值都救不回来，因为排序本身错了。** 原因在于 Jaccard 除以并集，于是惩罚一切长度差异——而用户的短查询，几乎永远是某个更长问法的碎片。重叠系数除以的是**较短**的那个集合，问的才是我们真正关心的问题：短的那个，是不是被包在长的里面？

**诚实的补充说明。** 重叠系数把排序修对了，但并**没有**干净地把最后两行分开——真配对和假配对都恰好落在 0.571。表面相似度本身无法区分它们。这不是调阈值能解决的问题，而是字符 n-gram 在短中文文本上的固有极限。阈值通过**偏向精确率**来应对，而被牺牲的召回率由下面的单链接聚类补回来。

由 `tests/test_dedup.py::test_jaccard_ranks_the_false_pair_higher` 钉住——谁要是把度量换回去，这个测试会失败并给出解释。

---

## 2. 阈值取 0.6，是扫出来的

在样本日志上扫 `--dedup-threshold`，并逐个人工检查每一个被合并的簇：

| 阈值 | 合并出的簇 | 折叠掉的行数 | 结论 |
| --- | --- | --- | --- |
| 0.50 | 6 | 14 | 把 `支持哪些登录方式` 和 `支持哪些支付方式` 合并了——它们是不同的问题 |
| 0.55 | 4 | 11 | 同一对假配对仍然被合并 |
| **0.60** | **3** | **9** | 在样本日志上没有假配对 |
| 0.65 | 2 | 5 | 开始漏掉真正的重复 |
| 0.70 | 2 | 5 | — |
| 0.80 | 1 | 3 | 大部分重复都活了下来，去重几乎等于没做 |

0.6 是精确率干净、同时还没有丢掉关键簇的那个点。

**这是在一份 42 行的样本上调出来的，应当当作起点，而不是自然常数。** 正因如此 `--dedup-threshold` 是暴露出来的。真实部署应该拿自己流量上人工标注的配对再扫一遍——这其实就是这个工具对输出质量做的那件事，只不过反过来用在了工具自己身上。

---

## 3. 聚类跑在整份日志上，不只是候选集

最容易想到的做法是：给每条 trace 打分，留下高于最低分的，再对这份短名单去重。它会产出一个**不诚实的数字**。

如果一个被问了 200 次的问题只崩过 1 次，「先筛后去重」会报「出现 1 次」。「先聚类整份日志」会报「出现 200 次」——而后者才是你能真正写进报告的那句话：*这个用例代表着我们记录到的 200 次调用。*

所以流程是：给全部打分 → 给全部聚类 → 提拔那些确实包含东西的簇。每个簇的代表是它分数最高的成员，因为排序保证了最糟的那一行最先认领这个簇。

---

## 4. 用单链接（single linkage），不是质心

一个簇会跟它的**每一个成员**比对，而不只是跟代表比。

不做这件事，链就会断。`我想了解退款政策` 跟那个长的代表 `你们的退款政策是什么？` 只有 0.43，但跟已经在簇里的、更短的 `请问退款政策` 有 0.60。用质心比对会丢掉它，用单链接能留住。

代价是相似度不满足传递性，所以一个松散的簇可能靠链式反应越滚越大。在这里由 `min_shared` 兜住，并且因为失败 trace 排在最前面、会早早地把簇锚定下来，所以风险是受控的。

---

## 5. 用字符二元组，不用分词

同一套代码路径必须同时服务中文和英文。中文没有空格分词边界，要按词切就得引入分词器和词典——对于一个把「零依赖」当卖点的工具来说，这个包袱太重。

用二元组而不是三元组，是因为用户查询都很短：一个五字的问题切三元组之后，几乎剩不下什么可以匹配的东西。

**代价：** 英文改写场景的召回率弱。`"how do I get a refund"` 和 `"refund process"` 有共享的字符 n-gram，会匹配上；`"getting my money back"` 不会。这条被写进了已知短板，而不是糊过去。

---

## 6. 空输出是信号，不是坏行

第一版对一次调用的两侧复用了同一个「这个值在不在」的辅助函数。它把 `"output": ""` 当成缺失字段，然后把这行跳过了。

那等于**悄悄删掉了这个工具存在的唯一理由**。空的**输入**才是坏行——没东西可答。空的**输出**是完全可读的一行，而且是日志里最有价值的行之一。

现在两种情况分开处理（`_pick(..., allow_empty=True)`），并由 `test_empty_output_is_a_trace_not_an_unreadable_line` 钉住。

---

## 7. 参考输出不等于标准答案

这是塑造整个用例集的那条决定。

大多数被选中的 trace，恰恰是因为它出了错才被选中的。所以从它的输出里推断「期望形状」，基本上就是在从失败里学标准。

规则：

- **干净 trace**（`negative_feedback`、`user_retried`、`empty_output`、`fallback_phrase`、`expected_shape_violated`、`output_much_shorter` 一个都没触发）：它的输出可以当作假定正确的参考。按它长度的 40% 推断 `min_chars`；如果它能解析成 JSON 对象，就推断出 schema。
- **失败种子**（上面任意一个触发了）：只断言 `not_fallback`，别的都不加。这个用例存在的意义，就是让这一次具体的失败永远不要上线第二次。

`min_chars` 故意放松到 40%。它的目的是抓**截断**，不是惩罚改写——一个会因为合理改写而失败的检查，活不过一个星期就会被关掉。

### 为什么不从参考输出推断 `contains`

很诱人，而且是错的。从某条参考输出里自动生成 `contains: ["14 个工作日"]`，等于把测试过拟合到某一种确切措辞上。之后任何一个正确但换了说法的回答都会失败，测试套件变成噪音，然后团队就不再信任它了。当某条 trace 自己带了显式的 `contains`，我们会照办——因为那时候这些词是人选的。

---

## 8. 不做 LLM 裁判

v0.1 主动拒绝了这个方案，三条理由：

1. **CI 里的成本和延迟。** 每个 PR 都跑一遍裁判模型，既是一笔账单，也是一个瓶颈——而且它是构建一旦变慢后第一个被关掉的东西。
2. **不确定性。** 这个工具的全部价值主张就是：同一份日志产出同一套用例。裁判会破坏这一点。
3. **这件事已经被解决了。** DeepEval、Ragas、promptfoo 都把基于裁判的指标做得很好。在这里重造，正是这个项目想避开的那个坑——「又一个评估框架」。

`trace2eval` 负责产出用例；它不假装能评判开放式质量。确定性检查是评估里最枯燥的那一半，而最枯燥的那一半，才是真正能在每次提交时都跑起来的那一半。

---

## 9. 回归容差要么是绝对百分点，要么是相对百分比

通过率和格式合规率用**绝对百分点**判定（2pp），因为「我们掉了 2% 的通过用例」在任何基线下含义都一样。

p95 延迟和平均成本用**相对百分比**判定（20%），因为「慢了 20%」「贵了 20%」要跟着现有数字的规模走。

`--tolerance-scale` 会统一放大或缩小所有容差，所以想让门禁更严或更松，都不需要改代码。

---

## 接下来做什么

按「对工具的提升幅度」排序，不按「有没有意思」排序。

1. **让失败种子也能带上形状，从干净答案里推导。** 今天一个 `fallback_phrase` 种子只断言「不许兜底」——一个**错得不一样**的答案照样能过。如果同一个问题在日志别处有干净的答案，`min_chars` 就应该从**那些**答案推导，而不是从那个坏的参考输出。这是最大的缺口。样本日志里就复现了它：把某个失败种子用例的答案改成 `订单处理中。`，门禁抓不到。
2. **给发生次数统计加 blocking key。** O(用例数 × 日志大小) 在一万行以内没问题。给簇的候选集建一层倒排索引，就能在不改变语义的前提下撑到百万级。
3. **建一个带标注的去重评测集。** 现在阈值是在 42 行上调的。几百对人工标注的样本、在每个阈值上算精确率和召回率，就能把一个「合理的猜测」变成一个「站得住的默认值」——而且这本身就是这个工具在吃自己的狗粮。
